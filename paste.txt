<Dialog.Container
            visible={isCompanyVisible}
            contentStyle={{ backgroundColor: '#fff', padding: 0, borderRadius: 10 }}>
            <Dialog.Title style={{ color: '#fff', backgroundColor: '#1a55e3', padding: 10, textAlign: 'center', borderRadius: 10, ontSize: 16 }}>Application Setting</Dialog.Title>
            <Text style={{ color: '#000', marginLeft: 20, paddingTop: 5, fontSize: 14, fontWeight: '500' }}>Please Enter Company Name/Code</Text>
            <View style={styles.AppSettinginputView}>
              <View style={{ width: '100%', }}>
                <TextInput
                  style={{ width: '100%', color: '#000' }}
                  label="Company"
                  placeholder="Enter Code"
                  value={companycode}
                  onChangeText={text => {
                    company.current = companycode
                    company.current = text // Added by Vinit 17-09-2025
                    setCompanyCode(text.trim())
                    console.log("company.current--2", company.current)
                  }}
                />
              </View>
            </View>
            <View style={{ width: '100%', alignContent: 'center' }}>
              {
                DiaLoading ?
                  <ActivityIndicator size={30} color="#1a55e3" /> :
                  <Dialog.Button
                    style={{ fontSize: 14, width: '40%', backgroundColor: '#1a55e3', margin: 10, color: '#fff', alignContent: 'center', justifyContent: 'center', borderRadius: 10 }}
                    label="CONTINUE" onPress={() => {
                      company.current = companycode
                      handleCompanyCodeAPI()
                      console.log("company.current--2", company.current)
                    }}
                  />
              }
            </View>
          </Dialog.Container>
