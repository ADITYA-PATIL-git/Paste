{pendingActionData.map((item, index) => (

                    <View key={index} style={{
                      height: 250,
                      borderRadius: 30,
                      backgroundColor: 'pink',
                      width: '40%',
                      padding: 20,
                      justifyContent: 'space-between'
                    }}>
                      <Text>
                        ICON
                      </Text>
                      <View>
                        <Text style={{ fontSize: width * 0.1, color: 'black' }}>{item?.Count}</Text>
                        <Text style={{ fontSize: width * 0.04, color: 'black' }}>{item?.PageName}</Text>
                      </View>
                    </View>
                  ))}
