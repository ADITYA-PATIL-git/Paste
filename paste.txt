import React from 'react';
import { StyleSheet, Text, View, ScrollView } from 'react-native';
import { SafeAreaView, SafeAreaProvider } from 'react-native-safe-area-context';
import Header from '../components/Header';
import ClockedHoursBox from '../components/ClockedHoursBox';
import NewAnniversaryBox from '../components/NewAnniversaryBox';
import NewAnnouncementBox from '../components/NewAnnouncementBox';
import PendingActionsBox from '../components/pendingActionsBox';
import OTBreachBox from '../components/OTBreachBox';

const Attendance = () => {
  return (
    <SafeAreaProvider>
      <SafeAreaView style={styles.container} edges={['top']}>
        <ScrollView style={styles.scrollView}>
          <View style={{ height: 10 }} />
          <ClockedHoursBox />
          <View style={{ height: 10 }} />
          <NewAnniversaryBox name={'Sakshi'} />
          <View style={{ height: 10 }} />
          <NewAnnouncementBox />
          <View style={{ height: 10 }} />
          <PendingActionsBox />
          <View style={{ height: 10 }} />
          <OTBreachBox />
          <View style={{ height: 100 }} />
        </ScrollView>
      </SafeAreaView>
    </SafeAreaProvider>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
  },
  scrollView: {
    paddingRight: 10,
    paddingLeft: 10,
    backgroundColor: 'white',
  },
});

export default Attendance;
