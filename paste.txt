<FlatList
                            ref={flatListRef}
                            ListFooterComponent={() => <View style={{ height: 300 }} />}
                            data={filteredData}
                            renderItem={({ item }) => (
                                <View style={styles.container2}>
                                    <View style={{ flexDirection: 'row', alignItems: 'center' }}>
                                        {item?.sThumbnailBase64 ==
                                            'The specified key does not exist.' ||
                                            item?.sThumbnailBase64 == '' ||
                                            item?.sThumbnailBase64 == null ||
                                            item?.sThumbnailBase64 == undefined ? (
                                            <Image
                                                // source={{ uri: `data:image/jpeg;base64,${item?.sThumbnailBase64}` }}
                                                source={require('./images/dummy_profile_image.jpg')}
                                                style={styles.image1}
                                            />
                                        ) : (
                                            <Image
                                                source={{
                                                    uri: `data:image/jpeg;base64,${item?.sThumbnailBase64}`,
                                                }}
                                                // source={require('./images/dummy_profile_image.jpg')}
                                                style={styles.image1}
                                            />
                                        )}
                                        {/* <Image
                                    source={require('./images/dummy_profile_image.jpg')}
                                    style={styles.image1}
                                  /> */}
                                        <View
                                            style={{
                                                flexDirection: 'column',
                                                marginVertical: 15,
                                                width: '60%',
                                                backgroundColor: 'pink',
                                                marginRight: 2
                                            }}>
                                            <Text
                                                numberOfLines={2}
                                                style={[styles.EmpName, { color: '#fd8c8c' }]}>
                                                {item?.EMP_NAME ? item?.EMP_NAME : '--'}

                                            </Text>
                                            <Text style={styles.EmpSubtext}>
                                                {item?.DEPARTMENT_NAME ? item?.DEPARTMENT_NAME : '--'}
                                            </Text>
                                        </View>
                                        <Icon2 name="chat-outline" color="#000" size={28}
                                            onPress={() => {
                                                navigation.navigate("BirthdayChat", {
                                                    dataForChat: item
                                                })
                                            }}
                                        />
                                    </View>
                                </View>
                            )}
                        />
