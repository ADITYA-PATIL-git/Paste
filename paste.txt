<ScrollView
  horizontal
  showsHorizontalScrollIndicator={false}
>
  {/* List 1 */}
  {data1.map((item, index) => renderItem1({ item, index }))}

  {/* List 2 */}
  {data2.map((item, index) => renderItem2({ item, index }))}
</ScrollView>
