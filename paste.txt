{AnnouncementData == '' ? (
  <View style={{ width: '100%', flexWrap: 'wrap' }}>
    <Text style={{ color: 'white' }}>No announcement to display</Text>
  </View>
) : (
  <Carousel
    layout={'default'}
    style={styles.wrapper}
    data={translatedAnnouncements.length > 0 ? translatedAnnouncements : AnnouncementData}
    renderItem={renderImageItem}
    ref={carouselRef}
    sliderWidth={300}
    itemWidth={300}
    autoplay
    useScrollView
    loop
    autoplayInterval={5000}
  />
)}
