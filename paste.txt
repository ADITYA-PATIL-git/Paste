{LessAnniBdayCount != 0 || LessAnniBdayCount != '' ? (
            BirthLoad ? (
              <ActivityIndicator size='large' color="#1a55e3" style={{ height: 70, alignItems: 'center', justifyContent: 'center', width: '100%' }} />
            ) :
              <TouchableOpacity
                // style={[styles.block, styles.awardsBG]}
                onPress={() => {
                  var AnniversaryData1 = JSON.stringify(
                    Anni.EMP_LIST_Anniversary,
                  );
                  AnniversaryData1 != null
                    ? AsyncStorage.setItem('AnniversaryData', AnniversaryData1)
                    : AsyncStorage.removeItem(
                      'AnniversaryData',
                      AnniversaryData1,
                    );
                  var BirthdayData1 = JSON.stringify(Bday.EMP_LIST_Birthday);
                  BirthdayData1 != null
                    ? AsyncStorage.setItem('BirthDayData', BirthdayData1)
                    : AsyncStorage.removeItem('BirthDayData', BirthdayData1);
                  navigation.navigate('Birthday');
                }}>
                <View style={[styles.swipeBox, { marginTop: 15 }]}>
                  <View
                    style={{
                      flexDirection: 'row',
                      justifyContent: 'space-between',
                    }}>
                    <Text style={[styles.Bdaytext]}>{t("Birthday & Anniversary")}</Text>
                    {AnniBdayCount ? (
                      <Text style={[styles.ViewAll]}>
                        +{AnniBdayCount} {t("View All")}
                      </Text>
                    ) : LessAnniBdayCount ? (
                      <Text style={[styles.ViewAll]}>{t("View All")}</Text>
                    ) : (
                      <View></View>
                    )}
                  </View>

                  <View
                    style={{
                      flexDirection: 'row',
                    }}>
                    {BirthLoad ? (
                      <ActivityIndicator size='large' color="#1a55e3" style={{ height: 70, alignItems: 'center', justifyContent: 'center', width: '100%' }} />
                    ) : (
                      <View style={{ justifyContent: 'flex-start' }}>

                        <FlatList
                          showsHorizontalScrollIndicator={false}
                          horizontal={true}
                          data={BirthdayData}
                          renderItem={renderItem}
                        // keyExtractor={item =>item.id}
                        />

                      </View>
                    )}
                    <View style={{ justifyContent: 'flex-start' }}>
                      <FlatList
                        // contentContainerStyle={{flex: 1, flexDirection: 'Row'}}
                        showsHorizontalScrollIndicator={false}
                        horizontal={true}
                        data={AnniversaryData}
                        renderItem={renderItem1}
                      // keyExtractor={item =>item.id}
                      />
                    </View>
                  </View>
                </View>
              </TouchableOpacity>
          ) : (
            <View style={[styles.swipeBox, { marginTop: 15 }]}>
              <View
                style={{
                  flexDirection: 'row',
                  justifyContent: 'space-between',
                }}>
                <Text style={[styles.Bdaytext]}>{t("Birthday & Anniversary")}</Text>
              </View>
              <View style={{ alignItems: 'center', paddingBottom: 5 }}>
                <Text
                  style={{
                    fontFamily: 'Poppins Italic 400',
                    color: '#718093',
                  }}>
                  {t("No data available for today")}
                </Text>
              </View>
            </View>
          )}


const renderItem = ({ item }) => (
    <View
      style={{
        flex: 1,
        flexDirection: 'column',
        paddingBottom: 10,
        width: WIDTH * 0.23,
        alignItems: 'center',
        justifyContent: 'center',
      }}>
      {item?.sThumbnailBase64 == 'The specified key does not exist.' || item?.sThumbnailBase64 == '' || item?.sThumbnailBase64 == null || item?.sThumbnailBase64 == undefined ?
        <Image
          // source={{ uri: `data:image/jpeg;base64,${item?.sThumbnailBase64}` }}
          source={require('./images/dummy_profile_image.jpg')}
          style={styles.Bdayimage}
        /> :
        <Image
          source={{ uri: `data:image/jpeg;base64,${item?.sThumbnailBase64}` }}
          // source={require('./images/dummy_profile_image.jpg')}
          style={styles.Bdayimage}
        />
      }
      <View
        style={{
          backgroundColor: '#fff',
          position: 'absolute',
          top: 30,
          left: 20,
        }}>
        <Text
          style={{
            backgroundColor: '#fff',
            marginTop: 10,
            color: '#ff6348',
            position: 'absolute',
            fontSize: 10,
            paddingHorizontal: 10,
            paddingVertical: 1,
            textAlign: 'center',
            borderRadius: 10,
            fontFamily: 'Poppins SemiBold 600',
            shadowColor: '#636e72',
            shadowOffset: {
              width: 2,
              height: 2,
            },
            shadowOpacity: 0.25,
            shadowRadius: 3.84,
            elevation: 4,
          }}>
          Bday
        </Text>
      </View>
      <Text
        style={{
          color: '#1a1a1a',
          fontSize: 12,
          paddingTop: 15,
          textAlign: 'center',
          fontFamily: 'Poppins Bold 700',
        }}>
        {item?.EMP_NAME.split(' ')[0]}
      </Text>
    </View>
  );

  const renderItem1 = ({ item }) => (
    <View
      style={{
        flex: 1,
        flexDirection: 'column',
        paddingBottom: 10,
        width: WIDTH * 0.23,
        alignItems: 'center',
        justifyContent: 'center',
      }}>
      <Image
        source={require('./images/dummy_profile_image.jpg')}
        style={styles.Bdayimage}
      />
      <View
        style={{
          backgroundColor: '#fff',
          position: 'absolute',
          top: 30,
          left: 20,
        }}>
        <Text
          style={{
            backgroundColor: '#fff',
            marginTop: 10,
            color: '#7281fb',
            position: 'absolute',
            fontSize: 10,
            paddingHorizontal: 10,
            paddingVertical: 1,
            textAlign: 'center',
            borderRadius: 10,
            fontFamily: 'Poppins SemiBold 600',
            shadowColor: '#636e72',
            shadowOffset: {
              width: 2,
              height: 2,
            },
            shadowOpacity: 0.25,
            shadowRadius: 3.84,
            elevation: 4,
          }}>
          Anniv
        </Text>
      </View>
      <Text
        style={{
          color: '#1a1a1a',
          fontSize: 12,
          paddingTop: 15,
          textAlign: 'center',
          fontFamily: 'Poppins Bold 700',
        }}>
        {item?.EMP_NAME.split(' ')[0]}
      </Text>
    </View>
  );
