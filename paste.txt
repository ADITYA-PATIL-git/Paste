<DateTimePicker2
                          mode="date"
                          display="calendar"
                          value={SelectedFromDate2}
                          minimumDate={
                            new Date(
                              FDate.MinYear,
                              FDate.MinMonth - 1,
                              FDate.MinDays,
                            )
                          }
                          maximumDate={
                            new Date(
                              FDate.MaxYear,
                              FDate.MaxMonth - 1,
                              FDate.MaxDays,
                            )
                          }
                          onChange={(_event, date) => {
                            if (date) {
                              FromDateMethod(date);
                              setShow(false);
                            }
                          }}
                        />
