import React from 'react';
import {
  KeyboardAvoidingView,
  TextInput,
  StyleSheet,
  View,
  ScrollView,
  Text,
} from 'react-native';

const Leave = () => {
  const privilegeLeaveCount = 11;
  const usedPrivilegeLeaveCount = 2;
  const sickLeaveCount = 10;
  const usedSickLeaveCount = 3;
  const casualLeaveCount = 12;
  const usedCasualLeaveCount = 1;
  return (
    <KeyboardAvoidingView
      style={styles.container}
      behavior="height"
      keyboardVerticalOffset={110}
    >
      <ScrollView>
        <View
          style={{
            backgroundColor: '#fff',
            flexDirection: 'row',
            flexWrap: 'wrap',
            justifyContent: 'space-evenly',
            paddingVertical: '4%',

            borderRadius: 10,
            rowGap: 14,
          }}
        >
          <View
            style={{
              justifyContent: 'flex-start',
              flexDirection: 'column',
              borderRadius: 10,
              borderColor: 'grey',
              borderWidth: 1,
              paddingVertical: 10,

              width: '45%',
              minHeight: 90,
              paddingHorizontal: 10,
            }}
          >
            <Text>Privilege Leave</Text>
            <Text>{privilegeLeaveCount} days</Text>
            <Text>Used {usedPrivilegeLeaveCount} day's</Text>
          </View>
          <View
            style={{
              justifyContent: 'flex-start',
              flexDirection: 'column',
              borderRadius: 10,
              borderColor: 'grey',
              borderWidth: 1,
              paddingVertical: 10,

              width: '45%',
              minHeight: 90,
              paddingHorizontal: 10,
            }}
          >
            <Text>Sick Leave</Text>
            <Text>{sickLeaveCount} days</Text>
            <Text>Used {usedSickLeaveCount} day's</Text>
          </View>
          <View
            style={{
              justifyContent: 'flex-start',
              flexDirection: 'column',
              borderRadius: 10,
              borderColor: 'grey',
              borderWidth: 1,
              paddingVertical: 10,

              width: '45%',
              minHeight: 90,
              paddingHorizontal: 10,
            }}
          >
            <Text>Casual Leave</Text>
            <Text>{casualLeaveCount} days</Text>
            <Text>Used {usedCasualLeaveCount} day's</Text>
          </View>
        </View>
      </ScrollView>
    </KeyboardAvoidingView>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    padding: 14,
  },
  input: {
    height: 40,
    borderColor: 'gray',
    borderWidth: 1,
    marginBottom: 20,
    paddingHorizontal: 10,
  },
});

export default Leave;
