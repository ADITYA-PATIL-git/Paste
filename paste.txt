import * as React from 'react';
import {
  View,
  Text,
  Pressable,
  KeyboardAvoidingView,
  Image,
  Alert,
  ScrollView,
  Platform,
} from 'react-native';
import { useNavigation } from '@react-navigation/native';
import { SafeAreaView } from 'react-native-safe-area-context';
import CheckBox from '@react-native-community/checkbox';
import { useState } from 'react';
import LoginInput from '../components/LoginInput';
import { commonStyle } from '../styles/commonStyle';

import { encryptUsingAES256 } from '../Encryption';
import { decryptUsingAES256 } from '../decryption';
import { FetchHttpRequest } from '../src/FetchHttpRequest';
import AsyncStorage from '@react-native-async-storage/async-storage';
import {
  configureReanimatedLogger,
  ReanimatedLogLevel,
} from 'react-native-reanimated';
configureReanimatedLogger({
  level: ReanimatedLogLevel.warn,
  strict: false, // Reanimated runs in strict mode by default
});

const LoginScreen = () => {
  const navigation = useNavigation();
  const [companyCode, setCompanyCode] = useState('');
  const [username, setUsername] = useState('');
  const [password, setPassword] = useState('');
  const [toggleCheckBox, setToggleCheckBox] = useState(false);
  const [loading, setLoading] = useState(false);
  const [secure, setSecure] = useState(true);
  const [bday, setBday] = useState('');
  const [birthdayData, setBirthdayData] = useState([]);

  const handleLogin = async () => {
    if (!companyCode || !username || !password) {
      Alert.alert('Alert', 'All fields are required');
    } else {
      setLoading(true);

      var apiParamter = {
        sCompanyCode: companyCode.trim(),
        sUserName: username.trim(),
        sPassWord: password,
      };
      var encrypted = await encryptUsingAES256(apiParamter).toString();
      console.log('RefreshToken==encrypted==', encrypted);
      let Config = {
        Url: 'User/Mobile/AuthGenrate',
        body: {
          sInput: encrypted,
        },
        navigation,
      };
      FetchHttpRequest(Config)
        .then(async resp => {
          console.log('RefreshToken==resp==', resp);
          if (resp?.statusCode == '400') {
            setLoading(false);
            alert(resp?.statusMsg);
          } else if (resp == 'OracleConnection.ConnectionString is invalid') {
            setLoading(false);
            alert('Invalid Company Code');
          } else {
            var decrypt = decryptUsingAES256(resp);
            var d = JSON.parse(decrypt);
            var RefreshToken = d?.access_token;
            console.log('RefreshToken==', RefreshToken);
            AsyncStorage.setItem('RefreshToken', RefreshToken);
            // saveToken(RefreshToken);

            // getDeviceINFO();
            // getMsLink();
            await GetBirthdays();
            await doLogin();
          }
        })
        .catch(error => {
          console.log('RefreshToken=error=', error);
          setLoading(false);
        });
    }
  };

  async function doLogin() {
    let Appversion = '0.7';
    var apiParamter = {
      // sCompanyCode: compname1?.trim(),
      // sUserName: user1?.trim(),
      // sPassWord: passwrd,
      sCompanyCode: companyCode.trim(),
      sUserName: username.trim(),
      sPassWord: password,
      sSessionId: 'Mobile',
      // sUserId:'',
      sEmpCode: '',
      P_IP_ADDRESS: '',
      P_DEVICE_TYPE: Platform.OS,
      P_BROWSER_VERSION: Appversion,
      iCompId: '',
      P_BROWSER_TYPE:
        Platform.__constants.Manufacturer +
        ' ' +
        Platform.__constants.Model +
        ' ' +
        Platform.__constants.Release,
      P_IMEI_NO: '',
      sDeviceName: 'Mobile',
    };
    var encrypted = await encryptUsingAES256(apiParamter).toString();
    let Config = {
      // Url: 'User/GetUser',
      Url: 'User/GetLoginDeviceBinding',
      body: {
        sInput: encrypted,
      },
      navigation,
    };
    console.log('Config-----------------', Config);
    FetchHttpRequest(Config)
      .then(async resp => {
        console.log('Response ===', resp);
        if (resp?.statusCode == '400') {
          setLoading(false);
          alert(resp?.statusMsg);
          return { success: false, message: resp?.statusMsg };
        } else {
          var decrypt = decryptUsingAES256(resp);
          var data = JSON.parse(decrypt);

          // navigation.navigate('RootScreen', {
          //   userData: data,
          //   companyCode: companyCode,
          // });
          navigation.reset({
            index: 0,
            routes: [
              {
                name: 'RootScreen',
                params: {
                  userData: data,
                  companyCode: companyCode,
                  bday: bday,
                  birthdayData: birthdayData,
                },
              },
            ],
          });
          console.log(data);
          return { success: true, data };
        }
      })
      .catch(error => {});
  }

  async function GetBirthdays() {
    var apiParamter = {
      sCompanyCode: companyCode.trim(),
      P_COMP_ID: 307,
    };
    var encrypted = encryptUsingAES256(apiParamter).toString();
    let Config = {
      Url: 'User/GetBirthdays',
      body: {
        sInput: encrypted,
      },
      navigation,
    };
    FetchHttpRequest(Config)
      .then(async resp => {
        // console.log("GetBirthdays===",resp)
        if (resp != '' && resp != undefined) {
          var decrypt = await decryptUsingAES256(resp);
          var d = JSON.parse(decrypt);
          // console.log('GetBirthdays==resp=', d);
          setBday(d);
          var BirthdayData1 = d?.EMP_LIST_Birthday;
          console.log('Birthday List ====', BirthdayData1);
          setBirthdayData(BirthdayData1);
          console.log('LoginScreen ===', birthdayData);
          // var bdaycount = d?.BIRTHDAYS;
          // GetAnniversary(bdaycount);
          // setBirthLoad(false);
        } else {
          Toast.showWithGravity('Data Not Found', Toast.LONG, Toast.TOP);
          setLoading(false);
          // setBirthLoad(false);
        }
      })
      .catch(error => {
        // console.log("GetBirthdays==error===",error)
        // setBirthLoad(false);
      });
  }

  // useEffect(() => {
  //   GetBirthdays();
  // });

  return (
    <SafeAreaView
      style={{
        flex: 1,
        backgroundColor: '#2860E7',
      }}
      edges={['top', 'bottom', 'left', 'right']}
    >
      <KeyboardAvoidingView
        behavior="height"

        // enabled={keyViewEnabled}
        // ----------------------
      >
        <ScrollView style={{ backgroundColor: '#fff' }}>
          <View style={commonStyle.container}>
            <Text style={commonStyle.bigText}>Sign in to your Account</Text>
            <Text style={commonStyle.smallText}>
              Enter your login credentials
            </Text>
            <View style={{ height: 60 }} />
          </View>
          <View style={commonStyle.container2}>
            <View style={{ width: 150, height: 100 }}>
              <Image
                source={require('../assets/images/download.jpg')}
                style={{ width: '100%', height: '100%', resizeMode: 'contain' }}
              />
            </View>
            <LoginInput
              companyCode={companyCode}
              setCompanyCode={setCompanyCode}
              username={username}
              setUsername={setUsername}
              password={password}
              setPassword={setPassword}
              secure={secure}
              setSecure={setSecure}
            />

            <View
              style={{
                paddingLeft: 0,
                flexDirection: 'row',
                alignItems: 'center',
              }}
            >
              <CheckBox
                disabled={false}
                value={toggleCheckBox}
                onValueChange={newValue => setToggleCheckBox(newValue)}
                boxType={'circle'}
                tintColors={'#ffff'}
                onCheckColor={'#2860E7'}
              />
              <Text>Keep me signed in</Text>
            </View>
            <View style={{ paddingTop: 26, paddingLeft: 14, paddingRight: 14 }}>
              <Pressable
                style={commonStyle.loginButton}
                title={loading ? 'Logging in...' : 'Login'}
                onPress={handleLogin}
                // onPress={navigation.navigate('RootScreen')}
                disabled={loading}
              >
                <Text style={{ color: '#fff' }}>login</Text>
              </Pressable>
              <Pressable style={{ alignItems: 'center', paddingTop: 8 }}>
                <Text style={{ color: '#2860E7', fontWeight: 600 }}>
                  Forgot/Reset Password ?
                </Text>
              </Pressable>
            </View>
            <View
              style={{
                flex: 1,
                alignItems: 'center',
                justifyContent: 'flex-end',
                paddingBottom: 6,
              }}
            >
              <Text>Version</Text>
            </View>
          </View>
        </ScrollView>
      </KeyboardAvoidingView>
    </SafeAreaView>
  );
};

export default LoginScreen;
