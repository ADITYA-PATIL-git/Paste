import {
  translateText,
  downloadModelIfNeeded,
  deleteDownloadedModel,
  FirebaseTranslateLanguage,
} from '@react-native-ml-kit/translate-text';

export const translate = async (
  textToTranslate,
  sourceLanguage = FirebaseTranslateLanguage.ENGLISH,
  targetLanguage = FirebaseTranslateLanguage.GERMAN
) => {
  try {
    // Download model if needed
    await downloadModelIfNeeded(sourceLanguage);
    await downloadModelIfNeeded(targetLanguage);

    // Translate
    const translatedText = await translateText({
      text: textToTranslate,
      sourceLanguage,
      targetLanguage,
    });

    return translatedText;
  } catch (error) {
    console.error('Translation error:', error);
    throw error;
  }
};

// Optional: free storage
export const deleteModel = async (language) => {
  try {
    await deleteDownloadedModel(language);
    console.log('Model deleted successfully');
  } catch (error) {
    console.error('Failed to delete model:', error);
    throw error;
  }
};
