import React, { useEffect, useState } from "react";
import { Modal, View, Text, TouchableOpacity, FlatList, BackHandler, } from "react-native";
import { FetchHttpRequest } from "./FetchHttp/FetchHttpRequest";
import { useIsFocused } from '@react-navigation/native';
import AsyncStorage from '@react-native-async-storage/async-storage';
import { decryptUsingAES256 } from './decryption';
import { encryptUsingAES256 } from './Encryptoion';

const images = ["cat.png", "dog.png", "lion.png"]; // sample list

export default function ImagePopup({ visible, onClose, onSelect, navigation }) {
    const isFocused = useIsFocused();
    const [loading, setLoading] = useState(false);
    const [imageNamesList, setImageNamesList] = useState(null);

    const handleBackButton = () => {
        return true;
    };

    const setImageData = async () => {
        for (let i = 0; i < imageNamesList.length; i++) {
            try {
                const data = await GetImageData(imageNamesList[i].sIMMAGE_S3_BUCKET_FILE_NAME);
                console.log("Image loaded:", data);
            } catch (err) {
                console.warn("GetImageData failed for:", imageNamesList[i].sIMMAGE_S3_BUCKET_FILE_NAME, err);
            }
        }

    };

    useEffect(() => {
        if (imageNamesList) {
            setImageData();
        }
    }, [imageNamesList]);

    useEffect(() => {
        if (isFocused) {
            //   setAttachmentFag('')          
            setLoading(true);
            getData();
            BackHandler.addEventListener('hardwareBackPress', handleBackButton);
            return () => {
                BackHandler.removeEventListener('hardwareBackPress', handleBackButton);
            };
        }
    }, [isFocused]);


    const getData = async () => {
        CompanyID = await AsyncStorage.getItem('CompId');
        UserName = await AsyncStorage.getItem('UserName');
        UserId = await AsyncStorage.getItem('UserId');
        EmpId = await AsyncStorage.getItem('EmployeeId');
        SessionId = await AsyncStorage.getItem('SessionId');
        ConNo = await AsyncStorage.getItem('ContactNO');
        CompanyName = await AsyncStorage.getItem('COMPNAME');
        var SecondLeaveID = 'S';
        await AsyncStorage.setItem('SecondLeaveID', SecondLeaveID);
        var LeaveDayId = 'F';
        await AsyncStorage.setItem('LeaveDayId', LeaveDayId);
        // LeaveType();
        GetImageNames();
    };

    async function GetImageNames() {
        var param = {
            sCompanyCode: CompanyName,
            iIMMAGE_ID: null,
            sIMMAGE_CATEGORY: "Birthday",
            sUserId: UserId,
            sUserName: UserName,
            sSessionId: SessionId,
            sModuleId: ""
            // sModuleId: 384
        };

        console.log("GetImages==param==", param)
        var encrypted = await encryptUsingAES256(param).toString();
        console.log("GetImages==encrypted==", encrypted)
        const Config = {
            Url: 'Dashboard/MailCelebration/GetAll',
            body: {
                sInput: encrypted,
            },
            navigation
        };
        try {
            FetchHttpRequest(Config).then(async resp => {
                // console.log("Get_employee_roster==",resp)
                var decrypt = decryptUsingAES256(resp);
                var d = JSON.parse(decrypt);
                console.log("GetImages==res==", d)
                console.log("CHECK THIS ++++++++ ", d[0].sIMMAGE_S3_BUCKET_FILE_NAME)
                setImageNamesList(d);
                console.log("AUSHDOIAHWODIHAOWD +++++++++", d)

                // await AsyncStorage.setItem('FromD', d[0]?.CYCLE_FROM ? JSON.stringify(d[0]?.CYCLE_FROM) : '');
                // await AsyncStorage.setItem('ToD', d[0]?.CYCLE_TO ? JSON.stringify(d[0]?.CYCLE_TO) : '');
                // Fdate = await AsyncStorage.getItem('FromD');
                // FrDate = JSON.parse(Fdate);
                // Tdate = await AsyncStorage.getItem('ToD');
                // ToDate = JSON.parse(Tdate);
                // var UpdatedDate = selectedDate;
                // getDateData(UpdatedDate);


            });
        } catch (error) {

        } finally {
        }
    }

    async function GetImageData(sFileData) {
        var param = {
            FileName: sFileData,
            Identity: "Profile",
            sCompanyCode: CompanyName,
            sUserId: UserId,
            sUserName: UserName,
            sSessionId: SessionId,
            sModuleId: ""
        };

        console.log("GetImageData==param==", param)
        var encrypted = await encryptUsingAES256(param).toString();
        console.log("GetImageData==encrypted==", encrypted)
        const Config = {
            Url: 'DownloadLogo',
            body: {
                sInput: encrypted,
            },
            navigation
        };
        try {
            const resp = FetchHttpRequest(Config);
            const decrypt = decryptUsingAES256(resp);
            const d = JSON.parse(decrypt);
            console.log('GetImageData resp====', d)
            return d;


            // FetchHttpRequest(Config).then(async resp => {
            //     var decrypt = decryptUsingAES256(resp);
            //     var d = JSON.parse(decrypt);
            //     console.log("GetImageData==res==", d)
            //     console.log("CHECK THIS GetImageData++++++++ ", d[0].sIMMAGE_S3_BUCKET_FILE_NAME)
            //     set
            // });
        } catch (error) {
            console.log("GetImageData error", error)
            return null;

        }
    }

    return (
        <Modal visible={visible} transparent animationType="slide">
            <View style={{
                flex: 1,
                backgroundColor: "rgba(0,0,0,0.5)",
                justifyContent: "center",
                padding: 20
            }}>
                <View style={{ backgroundColor: "#fff", borderRadius: 10, padding: 20 }}>
                    <Text style={{ fontSize: 18, marginBottom: 10 }}>Select an Image</Text>

                    <FlatList
                        data={images}
                        keyExtractor={(item) => item}
                        renderItem={({ item }) => (
                            <TouchableOpacity
                                onPress={() => onSelect(item)}
                                style={{ padding: 10, borderBottomWidth: 1 }}
                            >
                                <Text>{item}</Text>
                            </TouchableOpacity>
                        )}
                    />

                    <TouchableOpacity
                        onPress={onClose}
                        style={{ marginTop: 15, padding: 10, backgroundColor: "#ddd" }}
                    >
                        <Text style={{ textAlign: "center" }}>Cancel</Text>
                    </TouchableOpacity>

                </View>
            </View>
        </Modal>
    );
}
