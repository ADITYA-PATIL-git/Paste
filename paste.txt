<View
                    style={{
                      flexDirection: 'row',
                    }}>
                    {BirthLoad ? (
                      <ActivityIndicator
                        size="large"
                        color="#1a55e3"
                        style={{ height: 70, width: '100%', justifyContent: 'center', alignItems: 'center' }}
                      />
                    ) : (
                      <ScrollView
                        horizontal
                        showsHorizontalScrollIndicator={false}
                        contentContainerStyle={{ flexDirection: 'row' }}
                      >
                        <FlatList
                          horizontal
                          scrollEnabled={false}  
                          showsHorizontalScrollIndicator={false}
                          data={BirthdayData}
                          renderItem={renderItem}
                        />

                        <FlatList
                          horizontal
                          scrollEnabled={false}   
                          showsHorizontalScrollIndicator={false}
                          data={AnniversaryData}
                          renderItem={renderItem1}
                        />
                      </ScrollView>
                    )}
                  </View>
