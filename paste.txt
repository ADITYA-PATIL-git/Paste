<FlatList
                    data={RevokeDate}
                    horizontal
                    showsVerticalScrollIndicator={false}
                    renderItem={({ item, index }) => (
                      <View
                        key={item.id}
                        style={{
                          flexDirection: 'row',
                          // backgroundColor: item?.checked == 'Y' ? '#1a55e3' : 'white',
                          borderWidth: 1,
                          paddingRight: 15,
                          paddingLeft: 5,
                          borderRadius: 5,
                          width: '100%',
                          flex: 1,
                          marginRight: 10,
                          marginVertical: 10,
                          borderColor: item?.STATUS == '05'
                            ? '#b6f8fc'
                            : item?.STATUS == '07'   //change by gokul
                              ? 'red'
                              : item?.STATUS == '06'
                                ? '#02cf51'
                                : '#d9d9d9',
                        }}>
                        <CheckBox
                          tintColors={{ true: '#1a55e3' }}
                          onCheckColor={
                            item?.STATUS == '05'
                              ? '#b6f8fc'
                              : item?.STATUS == '07'
                                ? 'red'
                                : item?.STATUS == '06'
                                  ? '#02cf51'
                                  : '#d9d9d9'
                          }
                          disabled={
                            // new change for pullback status 04
                            item?.SANDWICH == 1 || item?.STATUS == '06' || item?.STATUS == '05' || item?.STATUS == '04'
                              ? true
                              : false
                            // item?.STATUS.includes('Approved') ||
                            //   item?.STATUS.includes('Submit')
                            //   ? true
                            //   : false
                          }
                          value={item?.checked == 'Y'}
                          onValueChange={e => {
                            var TotalCount = 0;
                            TotalCount = TotalCount + count;
                            setRevokeDate(pre => {
                              let temp = [...pre];
                              let index = temp.indexOf(item);
                              temp[index]['checked'] = e.SANDWICH == 1 ? 'Y' : e ? 'Y' : 'N';
                              // temp[index]['checked'] = e ? 'Y' : 'N' &&  e.SANDWICH == 1 ? 'N':'N';
                              // var t=temp.filter((e)=>e[0].SANDWICH=='1')
                              if (temp[index + 1]?.SANDWICH == 1) {
                                for (var i = index; i < temp?.length; i++) {
                                  if (temp[i]?.SANDWICH == 1) {
                                    temp[i]['checked'] = e ? 'Y' : 'N';
                                  }
                                }
                              }
                              if (temp[index - 1]?.SANDWICH == 1) {
                                console.log("----")
                                for (var i = index - 1; i >= 0; i--) {
                                  if (temp[i]?.SANDWICH == 1) {
                                    temp[i]['checked'] = e ? 'Y' : 'N';
                                  }
                                }
                              }
                              console.log("Indx-----", temp[index - 1]?.SANDWICH)
                              console.log("t-----", temp)
                              return temp;
                            });
                            // RevokeDate.filter((item)=>item?.checked == 'Y').then(item.map(e=>e.leavE_DATE))
                            data = RevokeDate.filter(function (item) {
                              return item?.checked == 'Y';
                            }).map(function ({ LEAVE_DATE }) {
                              return { LEAVE_DATE };
                            });
                            data?.map(item =>
                              RevokeData.push({
                                Dates: item?.LEAVE_DATE,
                              }),
                            );
                            setRevokeSaveDate(RevokeData);
                            setRevokeDataLength(RevokeData.length);
                          }}
                        />
                        <View
                          style={{
                            alignItems: 'center',
                            justifyContent: 'center',
                          }}>
                          <Text
                            style={{
                              color:
                                item?.STATUS == '05'
                                  ? '#b6f8fc'
                                  : item?.STATUS == '07'
                                    ? 'red'
                                    : item?.STATUS == '06'
                                      ? '#02cf51'
                                      : '#000'
                            }}>
                            {item?.LEAVE_DATE}
                          </Text>
                        </View>
                      </View>
                    )}
                  />
