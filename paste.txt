const birthdayList = BirthdayData?.map(item => ({
  ...item,
  type: "birthday",
}));

const anniversaryList = AnniversaryData?.map(item => ({
  ...item,
  type: "anniversary",
}));


const combinedData = [...birthdayList, ...anniversaryList];


<FlatList
  horizontal
  showsHorizontalScrollIndicator={false}
  data={combinedData}
  keyExtractor={(item, index) => index.toString()}
  renderItem={({ item }) =>
    item.type === "birthday"
      ? renderItem(item)   // your birthday card function
      : renderItem1(item)  // your anniversary card function
  }
/>
