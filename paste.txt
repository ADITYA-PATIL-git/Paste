<LinearGradient
            colors={['#ba7af8', '#ed9285']}
            start={{ x: 0, y: 0 }}
            end={{ x: 1, y: 0 }}
            style={styles.imageWrapper}>
            <View style={styles.newProductRow}>
              <Icon
                style={styles.iconstyle}
                name="celebration"
                color="#fff"
                size={35}
              />
              {AnnouncementData == '' ?
                <View style={{ width: '100%', flexWrap: 'wrap' }}>
                  <Text style={{ color: 'white' }}>{t("No announcement to display")}</Text>
                </View> :
                <Carousel
                  layout={'default'}
                  style={styles.wrapper}
                  data={AnnouncementData}
                  renderItem={renderImageItem}
                  ref={carouselRef}
                  sliderWidth={300}
                  itemWidth={300}
                  autoplay
                  useScrollView
                  loop
                  autoplayInterval={5000}
                />}

            </View>

          </LinearGradient>
