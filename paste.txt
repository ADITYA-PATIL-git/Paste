import React, { useEffect, useState, useContext, useCallback } from 'react';
import { View, Text, Image, Dimensions, StyleSheet } from 'react-native';
import { useNavigation } from '@react-navigation/native';
import {
  createDrawerNavigator,
  DrawerContentScrollView,
  DrawerItem,
} from '@react-navigation/drawer';
import 'react-native-gesture-handler';
import { HeaderNotificationUser } from '../components/HeaderIcons';
import BottomTabNavigator from '../components/BottomTabNavigator';
import MyWorkSpace from './MyWorkSpace';
import DrawerButton from '../components/DrawerButton';
import Attendance from './Attendance';
import {
  getFocusedRouteNameFromRoute,
  useFocusEffect,
} from '@react-navigation/native';
import DetailScreen from './DetailScreen';
import SearchScreen from './SearchScreen';
import LeaveApplication from './LeaveApplication';
import Leave from './Leave';
import { createNativeStackNavigator } from '@react-navigation/native-stack';
import { SearchContext } from '../context/SearchContext';
import { encryptUsingAES256 } from '../Encryption';
import { decryptUsingAES256 } from '../decryption';
import { FetchHttpRequest } from '../src/FetchHttpRequest';
import AsyncStorage from '@react-native-async-storage/async-storage';
const { width } = Dimensions.get('window');

<BottomTabNavigator />;

const RootScreen = ({ route }) => {
  const { userData, companyCode } = route.params;
  const Stack = createNativeStackNavigator();
  const Drawer = createDrawerNavigator();
  const navigation = useNavigation();

  const [loading, setLoading] = useState([]);
  const [profileData, setProfileData] = useState([]);
  const { query, setQuery } = useContext(SearchContext);

  useEffect(() => {
    if (userData?.data?.Original_Photo) {
      EmployeeProfilePhoto();
    }
  }, [userData]);

  async function EmployeeProfilePhoto() {
    var param = {
      FileName: userData.data.sProfilePhoto,
      Identity: 'Profile',
      sCompanyCode: companyCode.trim(),
    };
    console.log('EmployeeProfilePhoto==param=', param);
    var encrypted = encryptUsingAES256(param).toString();
    console.log('EmployeeProfilePhoto==encrypted=', encrypted);
    let Config = {
      Url: `DownloadLogo`,
      body: {
        sInput: encrypted,
      },
      navigation,
    };
    FetchHttpRequest(Config).then(async resp => {
      // console.log('EmployeeProfilePhoto==resppp=', resp);
      AsyncStorage.setItem('base64', resp?.data);
      // if (resp?.toLowerCase().includes('could not find a part of the path')) {
      //   setProfileData('')
      //   setisLoading(false);
      // }
      // else
      if (resp != '') {
        setProfileData(resp?.data);
        setLoading(false);
      } else if (resp?.data == 'The specified key does not exist.') {
        setProfileData(resp?.data);
      } else {
        Toast.showWithGravity('Profile Image Not Found', Toast.LONG, Toast.TOP);
        setLoading(false);
      }
    });
  }

  function CustomDrawerContent(props) {
    return (
      <DrawerContentScrollView {...props}>
        <Image
          style={styles.image1}
          source={{ uri: `data:image/jpeg;base64,${profileData}` }}
        />

        <DrawerItem
          label="Home Screen"
          onPress={() => props.navigation.navigate('HomeScreenTabs')}
        />
        <DrawerItem
          label="Workspace"
          onPress={() =>
            props.navigation.navigate('Workspace', { screen: 'Workspace' })
          }
        />
      </DrawerContentScrollView>
    );
  }

  function SearchStack() {
    return (
      <Stack.Navigator
        screenOptions={{
          headerShown: false,
        }}
      >
        <Stack.Screen name="SearchScreen" component={SearchScreen} />
        <Stack.Screen name="DetailScreen" component={DetailScreen} />
      </Stack.Navigator>
    );
  }

  return (
    <Drawer.Navigator
      screenOptions={{
        drawerStatusBarAnimation: 'fade',
        drawerType: 'slide',
        overlayColor: 'transparent',
        sceneStyle: { backgroundColor: '#eef2fe' },
        headerTintColor: '#fff',
        headerShadowVisible: false,
        title: false,
        headerRight: () => <HeaderNotificationUser />,
        headerLeft: () => <DrawerButton />,
        freezeOnBlur: true,
      }}
      drawerContent={props => <CustomDrawerContent {...props} />}
    >
      <Drawer.Screen
        name="HomeScreenTabs"
        options={({ route }) => {
          const routeName = getFocusedRouteNameFromRoute(route) ?? 'Home';
          return {
            headerStyle: {
              backgroundColor: '#2860E7',
              borderBottomLeftRadius: routeName === 'Home' ? 0 : 14,
              borderBottomRightRadius: routeName === 'Home' ? 0 : 14,
            },
            headerTitle: routeName,
          };
        }}
      >
        {props => (
          <BottomTabNavigator
            {...props}
            userData={userData}
            profileData={profileData}
          />
        )}
      </Drawer.Screen>
      <Drawer.Screen
        name="Workspace"
        component={MyWorkSpace}
        options={{
          headerStyle: { backgroundColor: '#2860E7' },
        }}
      />
      <Drawer.Screen
        name="SearchStack"
        component={SearchStack}
        options={{
          headerStyle: { backgroundColor: '#2860E7' },
        }}
      />
    </Drawer.Navigator>
  );
};

const styles = StyleSheet.create({
  image1: {
    borderRadius: 100,
    height: width * 0.2,
    width: width * 0.2,
    marginLeft: width * 0.028,
  },
});

export default RootScreen;
