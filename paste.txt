import React from 'react';
import Box from './Box';
import { Text, View } from 'react-native';
import BoxHeader from './BoxHeader';
import RootScrollView from './RootScrollView';
import ImageBox from './ImageBox';
import AnniversaryUserElement from './AnniversaryUserElement';
import HeaderBox from './HeaderBox';

const userImagePath = '../assets/images/user.jpg';
const userName = 'Sakshi';

const NewAnniversaryBox = ({ birthdayData, userData, anniversaryData }) => {
  console.log('NewAnniversaryBox ===', anniversaryData);
  return (
    <HeaderBox
      count={birthdayData.length}
      children={birthdayData.map((item, index) => (
        <AnniversaryUserElement
          index={index}
          userImage={item.PROFILE_PHOTO}
          userName={item.EMP_NAME}
        ></AnniversaryUserElement>
      ))}
    />
  );
};

export default NewAnniversaryBox;
